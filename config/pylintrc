[MESSAGES CONTROL]
disable=
  invalid-name,
  missing-docstring,
  too-few-public-methods,
  bad-continuation,
  fixme,  # this is manually handled
  wrong-import-order, # this should be handled by isort
max-line-length=120
extension-pkg-whitelist=ujson

# vim: set ft=config :
